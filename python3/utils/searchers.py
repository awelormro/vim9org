# vim: set foldmethod=indent:
# vim: set nospell:
import vim
import re


def SearchUp(pattern):
    """Objective:
        Find a string with a specific char upside
    """
    curr_pos = vim.eval("line('.')")
    if curr_pos()
    final_pos = 0
    return final_pos


def SearchDown(pattern):
    pass



